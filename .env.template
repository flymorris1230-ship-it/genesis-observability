# Genesis Observability - Environment Variables Template
# Copy this file to .env and fill in your actual values

# ============================================================================
# LLM API Keys (for development/testing)
# ============================================================================

# OpenAI API Key
# Get it from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# Anthropic API Key
# Get it from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...

# Google Gemini API Key
# Get it from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=...

# ============================================================================
# Supabase Configuration
# ============================================================================

# Supabase Project URL
# Get it from: Supabase Dashboard → Settings → API → Project URL
SUPABASE_URL=https://your-project.supabase.co

# Supabase Anon Key (for client-side access)
# Get it from: Supabase Dashboard → Settings → API → Project API keys → anon (public)
SUPABASE_KEY=eyJ...

# Supabase Service Role Key (for server-side access with full permissions)
# Get it from: Supabase Dashboard → Settings → API → Project API keys → service_role (secret)
# ⚠️ WARNING: This key has full database access. Never expose it in client-side code.
SUPABASE_SERVICE_KEY=eyJ...

# ============================================================================
# Cloudflare Configuration
# ============================================================================

# Cloudflare API Token (for Wrangler deployments)
# Create at: https://dash.cloudflare.com/profile/api-tokens
# Required permissions: Workers Scripts - Edit
CLOUDFLARE_API_TOKEN=...

# Cloudflare Account ID
# Find it at: https://dash.cloudflare.com/ → Workers & Pages → Account ID (right sidebar)
CLOUDFLARE_ACCOUNT_ID=...

# ============================================================================
# obs-edge Worker Configuration
# ============================================================================

# Worker API Key (for authentication)
# Generate a secure random key: openssl rand -hex 32
# This will be set as a Cloudflare Worker secret
OBS_EDGE_API_KEY=...

# Worker URL (after deployment)
# Format: https://obs-edge.YOUR_SUBDOMAIN.workers.dev
OBS_EDGE_URL=https://obs-edge.flymorris1230.workers.dev

# ============================================================================
# obs-dashboard Configuration
# ============================================================================

# Dashboard Environment Variables (add to .env.production in apps/obs-dashboard)
NEXT_PUBLIC_OBS_EDGE_URL=https://obs-edge.YOUR_SUBDOMAIN.workers.dev
NEXT_PUBLIC_OBS_EDGE_API_KEY=...

# ============================================================================
# Optional: NAS PostgreSQL (for Factory OS integration)
# ============================================================================

# PostgreSQL Database URL (if using local/NAS database instead of Supabase)
NAS_DATABASE_URL=postgresql://user:password@nas-host:5432/genesis_obs

# ============================================================================
# Optional: JWT Secrets (if implementing custom authentication)
# ============================================================================

# JWT Secret (for token signing)
# Generate: openssl rand -base64 32
JWT_SECRET=...

# Refresh Token Secret
REFRESH_SECRET=...

# ============================================================================
# Optional: Upstash Redis (for caching/rate limiting)
# ============================================================================

# Upstash Redis URL
UPSTASH_REDIS_URL=...

# Upstash Redis Token
UPSTASH_REDIS_TOKEN=...

# ============================================================================
# Development Settings
# ============================================================================

# Node Environment
NODE_ENV=development

# Current Development Phase
CURRENT_PHASE=Phase 3

# ============================================================================
# Instructions
# ============================================================================

# 1. Copy this file:
#    cp .env.template .env
#
# 2. Fill in all values marked with "..."
#
# 3. For sensitive values (like API keys), use strong, random strings
#
# 4. Never commit .env to git (it's already in .gitignore)
#
# 5. For production, set environment variables in:
#    - Cloudflare Workers: Use `wrangler secret put` command
#    - Vercel: Use Dashboard → Settings → Environment Variables
#    - Supabase: Use Dashboard → Settings → API
#
# 6. Verify your setup:
#    ./scripts/verify-deployment.sh
